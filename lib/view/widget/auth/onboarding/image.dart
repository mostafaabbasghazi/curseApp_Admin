import 'package:flutter/material.dart';

class ImageIntro extends StatelessWidget {
  const ImageIntro({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset("assets/intro.png");
  }
}